# Reports

`latest.json` is created after the first successful advisory retrieval. An empty advisory list means only that the named source returned no advisory at retrieval time; it is not confirmation that a dependency is safe.
